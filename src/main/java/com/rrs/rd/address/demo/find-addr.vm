<html>
<head>
<title>Search similar address</title>
<style>
	body, td, th, div, p, h3 { font-size:12px; margin:0; padding:0; }
	td, th { border-collapse:collapse; border-left:1px solid #bbb; border-top: 1px solid #bbb; padding:1px 4px 1px 4px; }
	table { border-collapse:collapse; border-right:1px solid #bbb; border-bottom: 1px solid #bbb; }
	.left10 { margin-left:10px; }
	.top5 { margin-top:5px; }
	.top20 { margin-top:20px; }
	.bot10 { margin-bottom:10px; }
	.em1 { color:red; } 
	.em2 { color:orange; }
	.c { text-align:center; }
	table.c td, table.c th { text-align:center; }
	tr.c td, tr.c th { text-align:center; }
	h3 { font-size:14px; font-weight:600; color:blue; }
	
</style>
</head>
<body style="padding:5px 20px 0 20px;">

<div>
	<h3>与【$!{queryAddr.rawText}】相似的地址：</h3>
	<table>
		<tr class=c>
			<th>相似度</th>
			<th>地址ID</th>
			<th>详细地址</th>
		</tr>
		#foreach($vo in ${similarVOs})
		<tr>
			<td>$!{vo.similarity}</td>
			<td>$!{vo.address.id}</td>
			<td>$!{vo.address.rawText}</td>
		</tr>
		#end
	</table>
	<span>[$!{queryAddr.province.name} $!{queryAddr.city.name} $!{queryAddr.county.name}]共</span>
	<span class="em1">$!{totalDocCount}</span>
	<span>条历史地址</span>
	<span class="left10">搜索用时:</span><span class="em1">$!{elapsedTime}毫秒</span>
</div>

<div class="top20">
	<h3>输入地址的解析情况：</h3>
	<span>$!{queryAddr.rawText}</span><span class="left10">&gt;&gt;</span>
	<span class="left10">[省市区:</span><span class="em1">$!{queryAddr.province.name} $!{queryAddr.city.name} $!{queryAddr.county.name}</span>]
	#if($utils.notEmpty($queryAddr.towns))<span class="left10">[街道/乡镇:</span><span class="em1">#foreach($town in ${queryAddr.towns})$!{town} #end</span>]#end
	#if($utils.notEmpty($queryAddr.village))<span class="left10">[村庄:</span><span class="em1">$!{queryAddr.village}</span>]#end
	#if($utils.notEmpty($queryAddr.road))<span class="left10">[道路:</span><span class="em1">$!{queryAddr.road}</span>]#end
	#if($utils.notEmpty($queryAddr.roadNum))<span class="left10">[门牌号:</span><span class="em1">$!{queryAddr.roadNum}</span>]#end
	#if($utils.notEmpty($queryAddr.text))<span class="left10">[其它:</span><span class="em1">$!{queryAddr.text}</span>]#end
	#if($utils.notEmpty($queryAddr.buildingNum))<span class="left10">[楼号/房间号:</span><span class="em1">$!{queryAddr.buildingNum}</span>]#end
	<table class="top5 c">
		<tr><th>词条</th>#foreach($term in ${queryDoc.terms})<td class="em1 c">$!{term.text}</td>#end</tr>
		<tr><th>类型</th>#foreach($term in ${queryDoc.terms})<td class=c>$term.type</td>#end</tr>
	</table>
</div>

<div class="top20">
	<h3>相似地址的解析情况：</h3>
	#foreach($vo in ${similarVOs})
	<span>$!{vo.address.rawText}</span>
	<table class="bot10 c">
		<tr><th>词条</th>#foreach($term in ${vo.document.terms})<td>$!{term.text}</td>#end</tr>
		<tr><th>类型</th>#foreach($term in ${vo.document.terms})<td>$term.type</td>#end</tr>
	</table>
	#end
</div>
</body>
</html>